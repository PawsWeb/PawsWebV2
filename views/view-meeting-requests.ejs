<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Meeting Requests | PawsWeb</title>
    <link rel="icon" type="image/png" href="/images/pawsweb-icon.png">
    <link rel="stylesheet" href="/styles.css">
    <style>
        .request-cards {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            gap: 20px;
            margin-top: 20px;
        }

        .request-card {
            width: 30%;
            background-color: #f8f8f8;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .request-card h3 {
            margin-top: 0;
            font-size: 20px;
            color: #333;
        }

        .request-card p {
            font-size: 14px;
            color: #555;
            margin: 10px 0;
        }

        .request-card button {
            background-color: #ff7043;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }

        .request-card button:hover {
            background-color: #d84315;
        }
    </style>
</head>
<body>
    <header>
        <a href="/"><img src="/images/logo.png" alt="PawsWeb Logo" class="logo"></a>
        <nav class="nav-buttons">
            <a href="/"><button>HOME</button></a>
            <a href="/aboutus"><button>ABOUT US</button></a>
            <a href="/ourpets"><button>OUR PETS</button></a>
            <a href="/educational"><button>EDUCATIONAL</button></a>
            <a href="/faq"><button>FAQ</button></a>
        </nav>
        <a href="/login"><button class="login-button">LOGIN</button></a>
    </header>

    <hr class="line">

    <main>
        <h2>View Meeting Requests</h2>

        <% if (requests.length > 0) { %>
            <div class="request-cards">
                <% requests.forEach(request => { %>
                    <div class="request-card">
                        <h3>Request for <%= request.animal_name %></h3>
                        <p><strong>Name:</strong> <%= request.name %></p>
                        <p><strong>Email:</strong> <%= request.email %></p>
                        <p><strong>Contact:</strong> <%= request.contact_number %></p>
                        <p><strong>Requested Date:</strong> <%= new Date(request.requested_date).toLocaleDateString() %></p>
                        <form action="/shelter/delete-meeting-request" method="POST">
                            <input type="hidden" name="requestId" value="<%= request.id %>">
                            <button type="submit" onclick="return confirm('Are you sure you want to delete this request?');">Delete</button>
                        </form>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>No meeting requests available.</p>
        <% } %>
    </main>

    <footer>
        <p>&copy; 2024 PawsWeb. All rights reserved.</p>
    </footer>
</body>
</html>
